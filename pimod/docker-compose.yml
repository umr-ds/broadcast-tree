version: "3"
services:
  pimod:
    image: nature40/pimod:v0.6.0
    privileged: true
    working_dir: /root
    volumes:
      - .:/root
      - ../.git:/root/.git
      - ../nexmon:/root/nexmon
      - ../userland:/root/btp
